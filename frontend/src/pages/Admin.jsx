import React from "react";

export default function Admin() {
  return (
    <div style={{
      display: "flex",
      height: "100vh",
      justifyContent: "center",
      alignItems: "center",
      fontSize: "1.5rem"
    }}>
      Admin Panel (coming soon)
    </div>
  );
}
